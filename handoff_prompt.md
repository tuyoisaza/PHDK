# PHDK — Project Handoff to Development Kit
## Final Generation Prompt v1.6

This prompt uses the reusable PHDK standards repo at `https://github.com/tuyoisaza/PHDK`.

Every generated file that references standards must point to this URL.

---

## Hard Mode Switch

STOP ALL PREVIOUS TASKS.

You are now entering **PHDK MODE — Project Handoff to Development Kit**.

Do not continue any previous task.

Do not generate UI, source code, implementation plans, app architecture changes, or product strategy.

Your only job from this point forward is to generate structured project-specific documentation files through the PHDK workflow.

Before asking any setup questions, first respond exactly:

```txt
Understood. I am now in PHDK MODE.
Previous tasks are paused.
Project identified: [project name from the conversation]
I will now ask you 4 questions before generating any files.
```

If the project name is unknown, use:

```txt
Project identified: ⚠️ GAP: Project name not clearly identified from the conversation.
```

After that confirmation, ask only Question 1.

If you do not provide the exact confirmation above, you have not entered PHDK MODE.

---

## Critical Operating Instruction

When this prompt is activated, do **not** create one generic document about PHDK.

Do **not** summarize the kit.

Do **not** generate all files at once.

Do **not** create a single file that contains every section.

Your job is to enter **PHDK generation mode**, ask the setup questions, then generate the requested files **one at a time as separate markdown files**.

Each generated file must be complete, standalone, and ready for the user to copy into a real repo using the exact filename provided.

The deliverable is not one document. The deliverable is a file-by-file generation workflow that produces the files listed in this prompt.

---

## Mission

You have already been briefed on this project through conversation, refinement, research, and planning.

Your task now is to stop ideating and convert everything you understand about this project into a **Project Handoff to Development Kit (PHDK)** by generating a sequence of separate markdown files.

This kit is not the app.  
This kit is not source code.  
This kit is not generic development standards.  
This kit is not the app foundation build.

This kit is the structured project-specific documentation a developer or AI coding agent will use to understand the project, prepare the repo, and start the first build task safely.

---

## Source of Truth

Use the existing conversation as the project brief.

Do not ask me to restate the brief.

The brief may include any combination of:

- product, personal, business, marketing, or service goals
- target users and user needs
- desired experience and content direction
- workflows, constraints, examples, positioning
- commercial expectations and design preferences

Use only what was discussed.

Do not invent features, users, pages, business logic, or requirements.

If something is missing or unclear, mark it inline:

`⚠️ GAP: [what is missing and why it matters]`

If you make a reasonable inference, mark it inline:

`Assumption: [what you inferred and why]`

Maintain an internal running list of every `⚠️ GAP` and every open question encountered while generating files. Use this list when generating `STATUS.md`.

These rules apply to every file. They are not repeated in individual file sections.

---

## Standards Repo

Generic development standards are not generated by PHDK.

They live in the reusable PHDK standards repo:

`https://github.com/tuyoisaza/PHDK`

Every generated file that references standards must point to this URL.

Required standards files the AI coder must fetch before doing anything:

- `AGENTS.md` — AI coder behavior rules
- `DEVELOPMENT_RULES.md` — Git, branching, commits, versioning
- `DESIGN_RULES.md` — Responsive, accessible, theming, performance
- `TECHNICAL_STACK.md` — Full stack, monorepo, deployment
- `QA_CHECKLIST.md` — Quality and testing standards
- `BUILD_APP_FOUNDATION_PROMPT.md` — Initial scalable app foundation prompt

Do not regenerate these files unless explicitly asked.

---

## Mandatory Technical Direction

Do not invent a stack.

All project-specific files assume this standard architecture unless I explicitly override it:

- pnpm + Turborepo + TypeScript monorepo
- `apps/web` — Next.js
- `apps/api` — NestJS + Fastify
- `apps/mobile` — Expo placeholder only
- `packages/*` — shared core
- Tailwind CSS + shadcn/ui compatible
- Zod used where needed
- Drizzle, PostgreSQL, and Redis ready, but not implemented unless the project phase requires them
- Stripe-ready when payments are needed, not before
- WorkOS or Clerk-ready when login is needed, not before
- OpenTelemetry-ready and Sentry-ready, not implemented until the relevant phase or task
- Railway deployment from `main` branch push
- Two Railway services: `@repo/web` and `@repo/api`
- Both Railway services use the repository root — never set root to `apps/web` or `apps/api`

The product brief controls what the product does.  
The standards repo controls how the product is built.  
`BUILD_APP_FOUNDATION_PROMPT.md` controls how the initial app foundation is created.

---

## Before Generating Any Files

Ask these questions one at a time.

Wait for each answer before asking the next.

Do not generate any files until all are answered.

Do not explain the whole process before asking.

After the required PHDK MODE confirmation, ask Question 1 using exactly this format:

```txt
Question 1:
Does this project require user login and accounts?

Answer options:
- yes
- no
```

### Question 1

Does this project require user login and accounts?

Answer options:

- yes
- no

### Question 2

Should this kit target MVP only, or MVP plus future phases?

Answer options:

- MVP only
- MVP + future phases

### Question 3

What brand and content direction should the kit use?

Include any of these if known:

- tone
- voice
- never sound like
- three brand words
- visual or color direction

Answer what you know, or say `skip`.

### Question 4

Is there anything you want to correct, add, or clarify about the project before I generate the kit?

Answer with the correction, addition, or clarification. Or say `go`.

After Question 4, `go` means begin generating only `PROJECT_BRIEF.md`.

After each generated file, the user must say `next` before you generate the next file.

Never proceed to the next file automatically.

---

## Login Branching Rule

### If login is required

- Generate `PRIVATE_CONTENT.md`
- Include auth, roles, account areas, private routes, and dashboard sections only where relevant
- Include login and account requirements in `PRD.md`, `FEATURES.md`, and `NAVTREE.md`
- Debug mode is controlled from the admin or config panel when admin/config exists

### If login is not required

- Skip `PRIVATE_CONTENT.md`
- Do not assume dashboards, user accounts, roles, admin panels, or user CRUD
- Keep the product focused on public content and public workflows
- Debug mode is environment-gated, not UI-controlled

---

## Debug Mode Rule

### If the project is app-style

If the project has interactive features, user flows, forms, dynamic behavior, or authenticated workflows, include debug mode as a P0 developer-support requirement in `FEATURES.md`.

This is not an end-user product feature. It must never appear in the customer-facing experience.

### If the project is static or public-only

If the project is a marketing site, landing page, or content site, include debug mode as a recommended technical note in `FEATURES.md`, not a P0 product requirement.

### Debug mode specification

- When active, functions report verbose structured logs
- A floating debug panel may appear only for authorized developer/admin contexts
- The debug panel shows the current version number
- The panel has two buttons:
  - **Copy Report** — copies version number plus a sanitized diagnostics report
  - **Clear Cache** — clears relevant browser/app cache, forces logout if applicable, forces cookie/file reload, and reloads the page
- If login/admin exists, debug mode is toggled from the admin or config panel
- If no login/admin exists, debug mode is toggled via environment variable or local developer config
- Debug mode is never active in production by default
- Debug reports must never copy secrets, tokens, passwords, private user data, payment data, raw server logs, or sensitive environment variables

---

## Generation Workflow

Generate one file at a time.

Treat each filename as a separate output artifact.

If the environment supports canvas or document artifacts, create a new separate canvas/document for each generated file. Do not stack multiple generated files inside one canvas/document.

If the environment does not support canvas or document artifacts, output the file content directly in the chat as a standalone markdown block.

Do not generate multiple files in one response.

Do not wrap all files into one master document.

Do not create a file named `PHDK.md`, `HANDOFF.md`, or similar unless explicitly requested.

Each response after setup must contain exactly one generated file.

Each generated file must be clean, self-contained, and copyable without requiring the user to manually split it from other files.

For each file:

1. If canvas/document artifacts are available, create a new separate canvas/document named exactly after the file, for example `PROJECT_BRIEF.md`.
2. If generating in chat, show the filename as the first line using an H1 heading, for example `# PROJECT_BRIEF.md`.
3. Generate the complete markdown content for that file only.
4. Do not include content for any other file.
5. Stop.
6. Show progress:

```txt
✅ Generated:
- [list of completed files]

⏳ Pending:
- [list of remaining files]
```

7. Say exactly:

`Save this file, then say next when you're ready to continue.`

### Correction loop

If I request edits to the current file, revise only that file, show the updated version, and wait for `next`.

Do not advance until I say `next`.

### Skip rule

If I say `skip` for a file, do not generate it.

Record the skipped file and reason in `STATUS.md`.

---

## Files to Generate

Generate these as separate files in this exact order.

Do not merge them.

Do not skip any file unless the login rule or user instruction says to skip it.

1. `PROJECT_BRIEF.md`
2. `PRD.md`
3. `FEATURES.md`
4. `NAVTREE.md`
5. `PUBLIC_CONTENT.md`
6. `PRIVATE_CONTENT.md` only if login = yes
7. `TASK.md`
8. `ARCHITECTURE_DECISIONS.md`
9. `STATUS.md`
10. `README.md`

---

## File Requirements

---

## PROJECT_BRIEF.md

Serves two audiences:

1. humans who need to understand the vision
2. AI coders who need immediate orientation

Open with this section first:

```md
## AI-Coder Summary

This project is: [one sentence]
This project is for: [target users]
The main user goal is: [what users come to do]
The business/service goal is: [what success looks like]
The MVP must deliver: [core deliverable]
The AI coder must not build: [out of scope]
Key gaps: [list or "none identified"]
Standards repo: https://github.com/tuyoisaza/PHDK
```

Then include:

- Problem being solved
- Proposed solution
- User value
- Vision
- Content expectations
- Design expectations
- Technical expectations based on standard stack
- Confirmed facts
- Assumptions
- Open questions

---

## PRD.md

Product Requirements Document.

Must include:

1. Summary
2. Problem
3. Confirmed Facts
4. Assumptions
5. Goals
6. Non-Goals / Out of Scope
7. Target Users
8. User Stories
9. Functional Requirements with P0 / P1 / P2 priorities
10. Non-Functional Requirements
11. User Flow
12. UX Requirements
13. Data Requirements
14. Success Metrics
15. Acceptance Criteria
16. Dependencies
17. Risks
18. Open Questions

Rules:

- Acceptance criteria must be testable
- If login is required, include auth and account requirements
- If login is not required, exclude all private account features
- Do not invent success metrics; mark missing metrics as gaps

---

## FEATURES.md

Defines what the product must actually do.

Must include:

- MVP features
- Phase 2 features only if scope includes future phases
- Explicitly out-of-scope features
- Priority for each feature: P0, P1, P2
- User value per feature
- Acceptance criteria for all P0 features
- Dependencies and risks
- Debug Mode section using the debug mode specification above

Rules:

- Features must be testable
- Do not include generic infrastructure unless it affects product behavior
- Do not include aspirational features unless marked as future

---

## NAVTREE.md

Defines the full navigation tree of the application.

Must include:

- Public navigation
- Private navigation only if login = yes
- Dashboard navigation only if login = yes
- Admin navigation only if clearly needed
- Role-based navigation only if clearly needed
- Mobile navigation notes
- Empty, loading, and error state expectations for major sections

Use a clear tree format.

---

## PUBLIC_CONTENT.md

Defines content visible before login, or the full site if no login.

Use brand personality from Question 3 to shape all content direction.

If Question 3 was skipped, include:

`⚠️ GAP: Brand personality not defined — content direction is generic.`

Must include:

```md
## Brand Voice Reference

Tone:
Voice:
Never sound like:
Three words:
Visual direction:
```

### Homepage

- Hero section — headline direction, subheadline direction, primary CTA
- Value proposition section
- Features or benefits section
- Social proof or trust section
- Secondary CTA

### Public Pages

- Each public page from `NAVTREE.md`
- Purpose of each page
- Key content blocks
- Primary CTA per page

### SEO

- Target keywords per page based on the brief only
- Meta title direction per page
- Meta description direction per page
- Heading structure notes
- Internal linking notes
- Gaps where keyword intent is unclear

### AEO — Answer Engine Optimization

- Key questions this product should answer for AI search tools
- Recommended FAQ structure and topics
- Structured data recommendations, such as FAQ schema, Product schema, or Organization schema when relevant
- Content blocks written to be directly citable by AI search engines
- Gaps where answer intent is unclear

### Calls to Action

- Primary CTA across the site
- Secondary CTAs
- CTA tone aligned to brand personality

### Trust Building

- Social proof approach
- Credibility signals
- Trust copy direction

### FAQ

- Recommended topics written to serve both users and AEO

### Assumptions and Open Questions

---

## PRIVATE_CONTENT.md

Generate only if login = yes.

Defines content visible after login.

Must include:

- Dashboard sections
- User account areas
- Feature-specific private pages
- Admin and config pages if applicable
- Role-based content differences if applicable
- Data shown to users
- Actions users can take
- Empty, loading, error, and success states

---

## TASK.md

Living file.

PHDK generates it once with the first task.

The user rewrites it before every subsequent coding session.

Must include two permanent sections.

### Section 1 — How to Use This File

```md
## How to Use This File

This file defines the current AI-coder session task.
Update this file before each new coding session.
Do not treat old tasks as active unless listed under Current Task.
```

### Section 2 — Current Task

```md
## Project Snapshot

[Brief project summary for AI coder orientation]

## Current State

[Where the project stands right now]

## Goal

[What this session must accomplish]

## Scope

[What the AI coder may touch]

## Out of Scope

[What the AI coder must not touch]

## Required Reading

1. TASK.md
2. STATUS.md
3. PROJECT_BRIEF.md
4. PRD.md
5. FEATURES.md
6. Standards repo files

## Standards Repo

https://github.com/tuyoisaza/PHDK

Required standards:

- AGENTS.md
- DEVELOPMENT_RULES.md
- DESIGN_RULES.md
- TECHNICAL_STACK.md
- QA_CHECKLIST.md
- BUILD_APP_FOUNDATION_PROMPT.md

## Acceptance Criteria

[Pass/fail checklist]

## Validation Commands

[Commands to run, or ⚠️ GAP: if unknown]

## Expected Final Report

Summary:
Changed files:
Validation:
Risks and assumptions:
Follow-up work:
```

The first generated task is always:

`Fetch the latest generic standards from the standards repo. Build the initial scalable app foundation using BUILD_APP_FOUNDATION_PROMPT.md. Report back with repo structure, quality gates, and confirmation.`

---

## ARCHITECTURE_DECISIONS.md

Records key decisions future AI agents must not undo.

Always include the debug mode decision as the first entry.

Use this format for each decision:

```md
## Decision: [Title]

Date:
Status: Proposed / Accepted / Rejected / Replaced
Context:
Decision:
Reason:
Consequences:
Related files:
```

Must include decisions for:

- PHDK standards repo as source of generic standards
- standard monorepo architecture
- Railway repo-root two-service deployment
- initial foundation created through `BUILD_APP_FOUNDATION_PROMPT.md`
- login/account decision
- debug mode decision

Include additional decisions only when supported by the conversation or the mandatory standard stack.

---

## STATUS.md

Living continuity file.

Generated at version zero.

Use the internal running GAP list and open questions collected during generation to populate this file accurately.

Must include:

```md
## Project Status

Project: [name]
Kit version: 0.0.0
Generated: [date]
Login required: yes / no
Scope: MVP only / MVP + future phases

## Current Status

Kit generated — not yet handed to coder.

## Files Generated

[list]

## Files Skipped

[list with reason, or "none"]

## Standards Repo

https://github.com/tuyoisaza/PHDK

## Required Standards Files

- AGENTS.md
- DEVELOPMENT_RULES.md
- DESIGN_RULES.md
- TECHNICAL_STACK.md
- QA_CHECKLIST.md
- BUILD_APP_FOUNDATION_PROMPT.md

## Gaps Flagged

[consolidated list of all ⚠️ GAP notes from all files, or "none"]

## Open Questions

[consolidated list of all open questions from all files, or "none"]

## Next Step

Hand kit to AI coder. Start with TASK.md.

## Last Updated

[date]
```

---

## README.md

Generate last.

Must include:

- What PHDK is
- What this kit is not
- Login status and scope for this project
- All generated files with one-line descriptions
- Files skipped and reason
- Recommended reading order for the AI coder
- Standards repo URL and which files to fetch before starting
- Clear note that `BUILD_APP_FOUNDATION_PROMPT.md` is the first build prompt after PHDK generation
- How to use `TASK.md` to start and manage coding sessions
- How to use `STATUS.md` to maintain continuity between sessions
- Next recommended step

---

## Quality Rules

- Write for a developer audience, not a business audience
- Be specific and actionable — no fluff, no filler
- Do not invent anything not discussed in the brief
- Separate confirmed facts from assumptions throughout
- Mark every assumption with `Assumption:`
- Flag every gap with `⚠️ GAP:`
- Keep MVP separate from future phases
- Do not generate app code
- Do not generate generic standards files
- Do not contradict the standards repo
- Do not ask me to repeat information already in the conversation
- Ask one question at a time, only when necessary to unblock generation

---

## Final Offer

After `README.md` is approved, say:

`🎉 PHDK complete. [N] files generated. All gaps and open questions are consolidated in STATUS.md. Would you like instructions to package everything into a ZIP file?`

If the environment supports file generation, offer to create the ZIP directly. Otherwise, provide packaging instructions.

